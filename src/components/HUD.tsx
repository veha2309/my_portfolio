import { useEffect, useState } from 'react';
export default function HUD() { const [progress, setProgress] = useState(0); useEffect(() => { const update = () => setProgress(innerHeight >= document.documentElement.scrollHeight ? 0 : scrollY / (document.documentElement.scrollHeight - innerHeight)); update(); addEventListener('scroll', update, { passive: true }); return () => removeEventListener('scroll', update); }, []); return <div className="chapter-progress" aria-hidden="true"><span style={{ transform: `scaleX(${progress})` }} /></div>; }
